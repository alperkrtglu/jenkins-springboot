@Library('jte-example') _

pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
       checkout scm
      }
    }

  stages {
    stage('Clean') {
      steps {
        sh './mvnw clean'
      }
    }

    stage('Build') {
      steps {
        buildAlp()
      }
    }

  }
}