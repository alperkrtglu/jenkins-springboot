@Library('jte-example') _

pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        scmAlp()
      }
    }

    stage('Build') {
      steps {
        buildAlp()
      }
    }

  }
}